import * as React from "react";
import * as ReactDOM from "react-dom";
import {inputsInterface} from './types';
import LifeCycle from './frp/lifeCycle';

/**
 * gather all cells and streams here
 * and injects them into react component
 * and then mount the app to dom;
 * @param inputs
 */
function pump(inputs: inputsInterface) {
}